# Commands & Skills

Quick reference for all available skills and commands in this project.

## Skills (in XX Skills/)
_No project-specific skills yet._

## Commands

### Correr local

```powershell
# Backend
cd balonpie-api
pip install -r requirements.txt
uvicorn main:app --reload

# Frontend
cd balonpie-frontend
npm install
npm run dev
```

> Verificá los nombres exactos de carpetas/scripts contra `AGENTS.md`/`package.json` si difieren.
